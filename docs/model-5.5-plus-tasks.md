# Model 5.5+ Tasks

This document collects follow-up items that should be handled in a later Model 5.5+ pass.

## Forecast Setup

- [x] Fix the `forecast_setup` page so the saved mapping summary appears immediately on refresh.
- [x] Remove the battery selection control from the `forecast_setup` page.
- [x] Ensure the page shows the configured map sensors from the saved Home Assistant config without waiting for the delayed entity refresh.
